# Environment Configuration for Attendly Backend

# Database Configuration
# Replace with your Supabase PostgreSQL connection string
# Format: postgresql://postgres:[YOUR-PASSWORD]@[HOST]:[PORT]/postgres
DATABASE_URL=postgresql://postgres:your_password@db.your-project.supabase.co:5432/postgres

# Secret Key for JWT
SECRET_KEY=your-secret-key-here

# Note: Get your Supabase connection string from:
# Supabase Dashboard -> Project Settings -> Database -> Connection String
# Make sure to use the "URI" format and replace [YOUR-PASSWORD] with your actual password

# Vector DB backend: chroma, faiss or firestore
VECTOR_DB_TYPE=firestore
VECTOR_DB_FALLBACK=chroma
CHROMA_PERSIST_DIRECTORY=./vector_db/chroma
FAISS_INDEX_PATH=./vector_db/faiss_index.pkl

# Firestore settings (only when using VECTOR_DB_TYPE=firestore)
# Set GOOGLE_APPLICATION_CREDENTIALS to the ABSOLUTE path of your service account JSON
# Example (PowerShell):
#   $env:GOOGLE_APPLICATION_CREDENTIALS = 'C:\full\path\to\service-account-key.json'
GOOGLE_APPLICATION_CREDENTIALS=F:\full\path\to\service-account-key.json
FIRESTORE_COLLECTION=face_encodings
GOOGLE_CLOUD_PROJECT=

# Face recognition settings
FACE_RECOGNITION_TOLERANCE=0.6
FACE_ENCODING_MODEL=large

# IMPORTANT: Do NOT commit your real `.env` or service account JSON to version control.
# Keep credentials out of the repository and use environment variables or secret managers.
