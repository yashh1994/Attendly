SECRET_KEY=your-super-secret-key-here
JWT_SECRET_KEY=your-jwt-secret-key-here

# PostgreSQL Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/attendly_db
# Alternative for development (comment out the above and uncomment below)
# DATABASE_URL=sqlite:///attendly.db

# PostgreSQL Connection Details (used for initial setup)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=attendly_db
POSTGRES_USER=username
POSTGRES_PASSWORD=iuMaWzi7fEb51kUQ

# Vector Database Configuration
VECTOR_DB_TYPE=chroma  # Options: chroma, faiss
CHROMA_PERSIST_DIRECTORY=./vector_db/chroma
FAISS_INDEX_PATH=./vector_db/faiss_index.pkl

# Face Recognition Settings
FACE_RECOGNITION_TOLERANCE=0.6
FACE_ENCODING_MODEL=large  # Options: small, large